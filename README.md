# P

## Codex installation

Use npm to install Codex globally. The latest version referenced here is `0.91.0`:

```bash
npm install -g @openai/codex@0.91.0
```

If you need the previous version for comparison or rollback, you can install `0.88.0`:

```bash
npm install -g @openai/codex@0.88.0
```
