# P

## Codex installation

Install a specific Codex version globally with npm:

```sh
npm install -g @openai/codex@0.87.0
```

Upgrade (or install) the newer release:

```sh
npm install -g @openai/codex@0.91.0
```
